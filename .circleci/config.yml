version: 2
jobs:
  build:
    working_directory: ~/hraftd
    docker:
      - image: circleci/golang:1.10


    steps:
        - checkout:
            path: ~/hraftd/src/github.com/otoolep/hraftd
        - run: go get -t -d -v ./...
        - run: go build -v

